# Customer registration resend v346

Adds a server-provided 60-second retry hint to the successful registration-mail redirect. The resend UI therefore starts disabled immediately after the first successful submission, including on a fresh render.
