# Customer style gallery grid v609

Protected runtime patch for the customer style-detail gallery.

- Uses a compact two-column mobile grid when two or more photos are present.
- Makes the first photo full width for odd photo counts so no empty final cell remains.
- Keeps single-photo layouts full width and preserves each photo's complete natural ratio.
- Leaves the desktop collage and staff style editor unchanged.
