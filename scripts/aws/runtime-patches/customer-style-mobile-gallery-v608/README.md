# Customer style mobile gallery v608

Protected runtime patch for customer style details.

- Shows each style photo at its complete natural aspect ratio on mobile.
- Removes the legacy `店舗スタイル / 担当` strip from customer details.
- Removes the duplicated lower menu section from customer details while preserving staff editing data.
- Preserves the v606 first-paint guard and the desktop gallery.
