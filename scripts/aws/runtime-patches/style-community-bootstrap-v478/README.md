# Style community bootstrap v478

Loads the v477 style-management client from the authenticated admin shell.

- The route observer is installed before staff click `スタイル共有`.
- First client-side navigation receives public/private and delete controls without a reload.
- Existing card images, APIs, visibility state, delete confirmation, and detail controls are unchanged.
- The page-level client remains as a guarded fallback for direct URLs.

Parent image: style community routed navigation v477 (`sha256:e876a1d3122b79aa519ad5ebaa9dbb5256adf2752b3d5f459b051659cebb416e`).
