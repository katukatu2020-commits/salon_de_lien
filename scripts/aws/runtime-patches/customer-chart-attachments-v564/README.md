# Customer chart attachments v564

- Exempts customer chart inputs from the global square-image guard.
- Accepts private JPG, PNG, WebP, and PDF chart attachments up to 20 MB.
- Provides separate short-lived inline-view and download URLs.
- Keeps customer chart files scoped to authenticated store accounts and their organization.
