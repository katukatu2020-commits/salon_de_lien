# Customer chart UI v562

- Rebuilds the latest chart-photo section around the image instead of leaving a large empty action column.
- Groups privacy, upload, history, registration time, uploader, and archive count into a clear hierarchy.
- Uses responsive desktop, tablet, and mobile layouts without changing chart-photo storage or permissions.
- Refines the chart-history grid for readable document thumbnails.
