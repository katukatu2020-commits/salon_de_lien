# Customer chart header actions v575

This runtime patch aligns the customer chart header actions without changing the shared admin layout.

- Resets the unintended top margin applied to the primary upload action.
- Keeps the history and upload buttons at the same 44 px height and vertical position.
- Applies the reset only inside customer chart views.
- Covers the upload action on the chart history page as well.
