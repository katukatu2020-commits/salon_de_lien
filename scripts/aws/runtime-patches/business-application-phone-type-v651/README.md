# Business application phone and type v651

- Requires a phone number in both browser and server validation for every new business application.
- Shows a required application-type selector on all business inquiry forms.
- Limits new applications to `salon` or `dealer` while preserving historical `other` and phone-less records.
- Redirects submission feedback to the page matching the selected application type.
