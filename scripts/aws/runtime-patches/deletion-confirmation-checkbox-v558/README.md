# Deletion confirmation checkbox v558

- Replaces typed deletion phrases with a clear confirmation checkbox.
- Keeps every destructive button disabled until the confirmation checkbox is selected.
- Applies the same behavior to style posts, style comments, and staff chat-message deletion.
- Preserves the existing server-side ownership and role authorization checks.
