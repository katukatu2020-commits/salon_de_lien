# Admin PC shell, settings and origin v422

Immutable child patch of AWS task 421. It removes the stale two-tab owner analytics flash, enforces the desktop admin shell at every viewport, keeps embedded settings responsive inside their dialog, and accepts only configured/canonical same-origin staff writes.
