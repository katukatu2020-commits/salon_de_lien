# Customer mobile bottom navigation v411

This immutable child patch keeps the current AWS v410 runtime intact and replaces only the shared customer experience asset.

It normalizes the native customer pages and Next.js customer pages to one 64px mobile bottom-navigation contract, with iPhone safe-area padding added exactly once.
