# Campaign image crop v519

Connects the campaign image field to the existing shared square-image cropper.

- Opens the established square crop dialog when a non-square campaign image is selected.
- Preserves drag positioning, zoom, JPEG export, and the existing campaign preview/upload flow.
- Keeps style-community image uploads excluded from square cropping.
- Does not create or submit campaign data during browser regression testing.
