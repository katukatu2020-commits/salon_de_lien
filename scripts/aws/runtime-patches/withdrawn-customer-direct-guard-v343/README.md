# Withdrawn customer direct guard v343

Prevents a store user from opening a withdrawn/nonexistent customer record via a stale direct URL. The request is redirected to the active customer list without rendering the legacy detail bundle.
