# Dealer order product UI v585

Improves the salon-side dealer product order table without changing pricing or order APIs.

- Groups list price, discount, and contract price into one scannable price column.
- Makes quantity controls visible and keeps their buttons inside the control boundary.
- Shows row subtotal and selected state while quantities change.
- Uses a five-column desktop table and a compact mobile card layout.
