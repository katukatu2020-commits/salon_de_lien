# Style admin pagination v647

- Shows 15 salon style posts per staff page so the five-column desktop grid ends on a complete third row.
- Keeps pagination in the current document without forcing the style list back to its top.
- Preserves the existing filters, manual ordering, visibility controls, deletion controls, thumbnail optimization, and 12-post customer page size.
