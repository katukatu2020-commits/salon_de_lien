# Public brand icon v511

This runtime patch replaces the legacy `L` letter mark in the public ORIMIA header with the same rainbow-hair icon used by the store and customer applications.

The existing home link and accessible label remain unchanged. The patch also removes the brown circular mark background and uses a cache-busted icon URL.
