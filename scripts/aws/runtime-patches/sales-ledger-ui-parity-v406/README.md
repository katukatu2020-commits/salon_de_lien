# Sales-ledger UI parity v406

Aligns the post-deployment sales ledger with the existing owner analytics and
billing presentation. It changes only the sales-ledger client stylesheet and
its cache key; search, correction, audit, printing, APIs, and database behavior
are unchanged.

The image is built from the exact immutable v405 production digest so no older
local application files replace the live AWS implementation.

