# Stamp reward pricing v645

- Keeps stamp-card redemption tied to its configured free menu from booking entry through checkout.
- Automatically selects the configured reward menu when the customer starts from the stamp card.
- Shows a zero-yen estimate only when the reward applies and explains an incompatible menu clearly.
- Suppresses the legacy banner that incorrectly claimed an incompatible stamp reward was already set.

Parent: `stamp-booking-rewards-v644` (`sha256:b22d0abc1e50d0c7bd153caee8b918393ac929188b04c4b20cc64c0f7c9cdca6`).
