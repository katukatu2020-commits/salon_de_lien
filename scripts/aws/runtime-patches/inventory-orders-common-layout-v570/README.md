# Inventory orders common layout v570

Routes the salon inventory and ordering workspace through the existing Next.js admin products page so it inherits the same authenticated sidebar, header, store controls, mobile navigation, and responsive layout as the rest of the salon application.

The wholesale ordering client and APIs remain unchanged. Its stylesheet foundation is scoped to wholesale content and dealer-owned pages so it no longer overrides the shared admin shell.
