# Staff image selection v467

スタッフ画像の正方形トリミング確定時に、モーダルの close イベントが先に `null` を返し、選択済みファイルを空へ戻していた競合を修正します。

- 確定・キャンセル・閉じるを一度だけ決着させます。
- 確定した `File` を解決してからトリミングモーダルを外します。
- 既存の DataTransfer、スタッフ保存API、private S3画像経路は変更しません。
- 現行本番 v466 のdigestだけを親にする保護リリースです。
