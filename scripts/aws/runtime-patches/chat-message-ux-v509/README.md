# Chat message UX v509

This runtime patch keeps chat bubbles within two thirds of the conversation width, forces long content to wrap, removes per-message edit/delete controls, and exposes confirmed deletion on double-click for messages owned by the signed-in sender.

The patch replaces the cache-busted v490 shared client with v509 while preserving the existing style-community controls.
