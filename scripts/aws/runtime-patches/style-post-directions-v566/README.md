# Style post directions v566

- Replaces the generic multi-image picker with fixed FRONT, SIDE, and BACK slots.
- Requires one valid image per direction for new posts and preserves legacy API payload compatibility.
- Persists ordered photo references and matching direction metadata on store style posts.
- Keeps the existing customer and staff community detail views compatible with older posts.
