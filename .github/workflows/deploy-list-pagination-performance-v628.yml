name: Deploy list pagination performance v628

on:
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

jobs:
  deploy:
    uses: ./.github/workflows/deploy-customer-store-unlink-v654.yml
    secrets: inherit
