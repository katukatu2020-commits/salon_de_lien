name: Deploy list pagination performance v628

on:
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

jobs:
  deploy:
    uses: ./.github/workflows/deploy-business-application-phone-type-v651.yml
    secrets: inherit
