name: Deploy list pagination performance v628

on:
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

jobs:
  deploy-style-admin-scroll-stability-v649:
    uses: ./.github/workflows/deploy-style-admin-scroll-stability-v649.yml
    secrets: inherit
