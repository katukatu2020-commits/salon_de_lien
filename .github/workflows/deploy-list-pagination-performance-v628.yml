name: Deploy list pagination performance v628

on:
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

jobs:
  deploy-business-application-rejection-v648:
    uses: ./.github/workflows/deploy-business-application-rejection-v648.yml
    secrets: inherit
