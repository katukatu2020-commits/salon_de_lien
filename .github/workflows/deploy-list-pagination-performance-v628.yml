name: Deploy list pagination performance v628

on:
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

jobs:
  deploy-style-admin-local-pagination-v650:
    uses: ./.github/workflows/deploy-style-admin-local-pagination-v650.yml
    secrets: inherit
